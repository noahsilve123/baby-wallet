# Render blueprint for the AI extractor container.
# Deploy with: render blueprint deploy render.yaml

services:
  - type: web
    name: destination-extractor
    env: docker
    plan: free
    dockerfilePath: extractor/Dockerfile
    healthCheckPath: /
    autoDeploy: true